<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.bsbm</groupId>
    <artifactId>bsbm-versioning</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>BSBM Versioning</name>
    <description>
        RDF version graph with a global merge policy (benchmark.versioning).
        Only the benchmark/versioning package is built by Maven; the rest of
        the BSBM sources keep the legacy Ant build (build.xml) and its jars
        under lib/.
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <jena.version>5.5.0</jena.version>
    </properties>

    <dependencies>
        <!-- Apache Jena: parses the generated PROV-O provenance.ttl (Turtle). -->
        <dependency>
            <groupId>org.apache.jena</groupId>
            <artifactId>jena-arq</artifactId>
            <version>${jena.version}</version>
        </dependency>
        <!-- SLF4J binding so Jena does not warn about a missing provider. -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>2.0.16</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- The project does not follow the src/main/java convention. -->
        <sourceDirectory>src</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <includes>
                        <include>benchmark/versioning/**/*.java</include>
                    </includes>
                </configuration>
            </plugin>
            <!-- Run the demo with: mvn compile exec:java -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>benchmark.versioning.Main</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>